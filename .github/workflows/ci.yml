name: CI

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest

      - name: Generate Dummy Artifacts
        run: |
          python -c "
          import os
          import joblib
          import numpy as np
          from sklearn.preprocessing import OneHotEncoder
          from sklearn.dummy import DummyClassifier

          # 1. إنشاء المجلد المطلوب
          os.makedirs('models_artifacts', exist_ok=True)

          # 2. حفظ الملفات داخل المجلد الصحيح
          encoder = OneHotEncoder(handle_unknown='ignore')
          encoder.fit([['A'], ['B']])
          joblib.dump(encoder, 'models_artifacts/encoder.joblib')

          model = DummyClassifier(strategy='most_frequent')
          model.fit(np.array([[1], [0]]), np.array([0, 1]))
          joblib.dump(model, 'models_artifacts/order_lateness_model.joblib')

          print('Dummy artifacts created successfully in models_artifacts!')
          "

      - name: Run tests
        run: pytest tests/test_preprocessing.py -v